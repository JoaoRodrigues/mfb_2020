name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # 3rd Party
  - numpy
  - matplotlib

    # Pip-only installs
  #- pip:
  #  - codecov

